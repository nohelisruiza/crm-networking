<!DOCTYPE html>
<html lang="es" data-theme="light">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EmpathyCRM - Digitalización de la Empatía</title>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #1a1a1a;
            --secondary: #c9a962;
            --accent: #f4f1ea;
            --text: #2d2d2d;
            --text-light: #6b6b6b;
            --white: #ffffff;
            --shadow: 0 4px 20px rgba(0,0,0,0.08);
            --shadow-hover: 0 8px 30px rgba(0,0,0,0.12);
            --danger: #e74c3c;
            --success: #27ae60;
            --warning: #f39c12;
            --bg-body: #f4f1ea;
            --bg-card: #ffffff;
            --border-color: rgba(201,169,98,0.1);
        }

        [data-theme="dark"] {
            --primary: #f4f1ea;
            --secondary: #d4b978;
            --accent: #2d2d2d;
            --text: #e0e0e0;
            --text-light: #a0a0a0;
            --white: #1a1a1a;
            --shadow: 0 4px 20px rgba(0,0,0,0.3);
            --shadow-hover: 0 8px 30px rgba(0,0,0,0.4);
            --bg-body: #121212;
            --bg-card: #1e1e1e;
            --border-color: rgba(201,169,98,0.2);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background: var(--bg-body);
            color: var(--text);
            line-height: 1.6;
            overflow-x: hidden;
            transition: background 0.3s, color 0.3s;
        }

        /* Header Elegante */
        header {
            background: var(--white);
            color: var(--primary);
            padding: 1.5rem 0;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: var(--shadow);
            transition: background 0.3s;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo-section {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .logo {
            font-family: 'Playfair Display', serif;
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--secondary);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .tagline {
            font-size: 0.8rem;
            color: var(--text-light);
            font-weight: 400;
            letter-spacing: 1px;
        }

        .header-actions {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .icon-btn {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            border: none;
            background: var(--accent);
            color: var(--text);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s;
            position: relative;
        }

        .icon-btn:hover {
            background: var(--secondary);
            color: var(--white);
            transform: scale(1.1);
        }

        .icon-btn .badge {
            position: absolute;
            top: -5px;
            right: -5px;
            background: var(--danger);
            color: white;
            font-size: 0.7rem;
            padding: 0.2rem 0.4rem;
            border-radius: 10px;
            font-weight: 600;
        }

        /* Main Layout */
        .app-container {
            display: grid;
            grid-template-columns: 380px 1fr;
            gap: 2rem;
            padding: 2rem;
            max-width: 1400px;
            margin: 0 auto;
            min-height: calc(100vh - 100px);
        }

        /* Sidebar */
        .sidebar {
            background: var(--bg-card);
            border-radius: 20px;
            padding: 2rem;
            box-shadow: var(--shadow);
            height: fit-content;
            position: sticky;
            top: 120px;
            border: 1px solid var(--border-color);
            transition: background 0.3s;
            max-height: calc(100vh - 140px);
            overflow-y: auto;
        }

        .form-header {
            margin-bottom: 1.5rem;
            padding-bottom: 1rem;
            border-bottom: 2px solid var(--accent);
        }

        .form-header h2 {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--primary);
            margin-bottom: 0.5rem;
        }

        .form-header p {
            font-size: 0.85rem;
            color: var(--text-light);
            font-style: italic;
        }

        .form-group {
            margin-bottom: 1.25rem;
        }

        label {
            display: block;
            font-size: 0.8rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 0.5rem;
            color: var(--primary);
        }

        input, textarea, select {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1px solid var(--accent);
            border-radius: 10px;
            font-family: 'Inter', sans-serif;
            font-size: 0.95rem;
            transition: all 0.3s;
            background: var(--bg-body);
            color: var(--text);
        }

        input:focus, textarea:focus, select:focus {
            outline: none;
            border-color: var(--secondary);
            box-shadow: 0 0 0 3px rgba(201,169,98,0.1);
        }

        textarea {
            resize: vertical;
            min-height: 80px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .btn {
            width: 100%;
            padding: 1rem;
            background: var(--primary);
            color: var(--bg-card);
            border: none;
            border-radius: 10px;
            font-size: 0.9rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .btn:hover {
            background: var(--secondary);
            transform: translateY(-2px);
            box-shadow: var(--shadow-hover);
        }

        .btn-secondary {
            background: transparent;
            color: var(--primary);
            border: 2px solid var(--primary);
            margin-top: 0.5rem;
        }

        .btn-secondary:hover {
            background: var(--primary);
            color: var(--bg-card);
        }

        .btn-success {
            background: var(--success);
        }

        /* Templates Section */
        .templates-section {
            margin-top: 1.5rem;
            padding-top: 1.5rem;
            border-top: 1px solid var(--accent);
        }

        .templates-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.5rem;
            margin-top: 0.5rem;
        }

        .template-btn {
            padding: 0.5rem;
            background: var(--accent);
            border: none;
            border-radius: 8px;
            font-size: 0.75rem;
            cursor: pointer;
            transition: all 0.3s;
            text-align: left;
            color: var(--text);
        }

        .template-btn:hover {
            background: var(--secondary);
            color: white;
        }

        /* Main Content */
        .main-content {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
        }

        .toolbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: var(--bg-card);
            padding: 1rem 1.5rem;
            border-radius: 15px;
            box-shadow: var(--shadow);
            flex-wrap: wrap;
            gap: 1rem;
            transition: background 0.3s;
        }

        .search-box {
            position: relative;
            flex: 1;
            min-width: 200px;
        }

        .search-box input {
            padding-left: 2.5rem;
            background: var(--bg-body);
        }

        .search-box i {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--text-light);
        }

        .filter-tags {
            display: flex;
            gap: 0.5rem;
            flex-wrap: wrap;
        }

        .tag-filter {
            padding: 0.5rem 1rem;
            background: var(--accent);
            border: none;
            border-radius: 20px;
            font-size: 0.8rem;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: 500;
            color: var(--text);
        }

        .tag-filter.active, .tag-filter:hover {
            background: var(--secondary);
            color: var(--white);
        }

        /* Contacts Grid */
        .contacts-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 1.5rem;
        }

        .contact-card {
            background: var(--bg-card);
            border-radius: 20px;
            padding: 1.5rem;
            box-shadow: var(--shadow);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            border: 1px solid var(--border-color);
            animation: fadeInUp 0.5s ease forwards;
        }

        .contact-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--shadow-hover);
        }

        .birthday-badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: linear-gradient(135deg, #f39c12, #e74c3c);
            color: white;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 600;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: start;
            margin-bottom: 1rem;
            padding-right: 80px;
        }

        .contact-info h3 {
            font-family: 'Playfair Display', serif;
            font-size: 1.25rem;
            color: var(--primary);
            margin-bottom: 0.25rem;
        }

        .contact-role {
            font-size: 0.85rem;
            color: var(--text-light);
            font-weight: 500;
        }

        .contact-avatar {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--secondary), #e8d5a3);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
            font-size: 1.2rem;
            box-shadow: 0 4px 10px rgba(201,169,98,0.3);
        }

        .card-body {
            margin-bottom: 1rem;
        }

        .detail-item {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            margin-bottom: 0.75rem;
            font-size: 0.9rem;
            color: var(--text);
        }

        .detail-item i {
            color: var(--secondary);
            width: 20px;
            text-align: center;
        }

        .personal-details {
            background: var(--accent);
            padding: 1rem;
            border-radius: 12px;
            margin-top: 1rem;
            border-left: 3px solid var(--secondary);
        }

        .personal-details h4 {
            font-size: 0.75rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: var(--text-light);
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .personal-details p {
            font-size: 0.9rem;
            color: var(--text);
            line-height: 1.5;
        }

        .card-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 1rem;
            border-top: 1px solid var(--accent);
        }

        .last-contact {
            font-size: 0.8rem;
            color: var(--text-light);
        }

        .card-actions {
            display: flex;
            gap: 0.5rem;
        }

        .action-btn {
            width: 36px;
            height: 36px;
            border-radius: 50%;
            border: none;
            background: var(--accent);
            color: var(--text);
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .action-btn:hover {
            background: var(--secondary);
            color: white;
            transform: scale(1.1);
        }

        .action-btn.delete:hover {
            background: var(--danger);
        }

        /* Modal Styles */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.6);
            backdrop-filter: blur(5px);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            opacity: 0;
            transition: opacity 0.3s;
        }

        .modal-overlay.active {
            display: flex;
            opacity: 1;
        }

        .modal {
            background: var(--bg-card);
            border-radius: 24px;
            padding: 2.5rem;
            max-width: 500px;
            width: 90%;
            box-shadow: 0 25px 50px -12px rgba(0,0,0,0.25);
            transform: scale(0.9);
            transition: transform 0.3s;
            position: relative;
            max-height: 90vh;
            overflow-y: auto;
        }

        .modal-overlay.active .modal {
            transform: scale(1);
        }

        .modal-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .modal-header h2 {
            font-family: 'Playfair Display', serif;
            font-size: 1.75rem;
            color: var(--primary);
            margin-bottom: 0.5rem;
        }

        .modal-header p {
            color: var(--text-light);
            font-size: 0.9rem;
        }

        .close-modal {
            position: absolute;
            top: 1.5rem;
            right: 1.5rem;
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--text-light);
            cursor: pointer;
            transition: color 0.3s;
        }

        .close-modal:hover {
            color: var(--primary);
        }

        /* Recording Interface */
        .recording-interface {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 2rem;
        }

        .record-button {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            border: 4px solid var(--secondary);
            background: var(--bg-card);
            cursor: pointer;
            position: relative;
            transition: all 0.3s;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .record-button:hover {
            transform: scale(1.05);
            box-shadow: 0 0 30px rgba(201,169,98,0.3);
        }

        .record-button.recording {
            background: var(--danger);
            border-color: var(--danger);
            animation: recordPulse 1.5s infinite;
        }

        @keyframes recordPulse {
            0% { box-shadow: 0 0 0 0 rgba(231, 76, 60, 0.4); }
            70% { box-shadow: 0 0 0 20px rgba(231, 76, 60, 0); }
            100% { box-shadow: 0 0 0 0 rgba(231, 76, 60, 0); }
        }

        .record-button i {
            font-size: 2.5rem;
            color: var(--secondary);
            transition: color 0.3s;
        }

        .record-button.recording i {
            color: white;
        }

        .timer {
            font-family: 'Playfair Display', serif;
            font-size: 2rem;
            color: var(--primary);
            font-variant-numeric: tabular-nums;
        }

        .modal-actions {
            display: flex;
            gap: 1rem;
            width: 100%;
        }

        .modal-actions button {
            flex: 1;
            padding: 0.875rem;
            border-radius: 10px;
            border: none;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s;
        }

        .btn-cancel {
            background: var(--accent);
            color: var(--text);
        }

        .btn-send {
            background: var(--secondary);
            color: white;
        }

        /* Settings Modal */
        .settings-section {
            margin-bottom: 2rem;
        }

        .settings-section h3 {
            font-size: 1rem;
            margin-bottom: 1rem;
            color: var(--primary);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .setting-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem;
            background: var(--accent);
            border-radius: 10px;
            margin-bottom: 0.5rem;
        }

        .toggle-switch {
            position: relative;
            width: 50px;
            height: 26px;
            background: var(--text-light);
            border-radius: 13px;
            cursor: pointer;
            transition: background 0.3s;
        }

        .toggle-switch.active {
            background: var(--secondary);
        }

        .toggle-switch::after {
            content: '';
            position: absolute;
            top: 3px;
            left: 3px;
            width: 20px;
            height: 20px;
            background: white;
            border-radius: 50%;
            transition: transform 0.3s;
        }

        .toggle-switch.active::after {
            transform: translateX(24px);
        }

        .import-export-btns {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .import-export-btns button {
            padding: 1rem;
            border-radius: 10px;
            border: 2px solid var(--secondary);
            background: transparent;
            color: var(--primary);
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 0.5rem;
        }

        .import-export-btns button:hover {
            background: var(--secondary);
            color: white;
        }

        .import-export-btns i {
            font-size: 1.5rem;
        }

        /* Birthday Modal */
        .birthday-list {
            max-height: 400px;
            overflow-y: auto;
        }

        .birthday-item {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            background: var(--accent);
            border-radius: 12px;
            margin-bottom: 0.75rem;
            border-left: 4px solid var(--secondary);
        }

        .birthday-item.today {
            border-left-color: var(--danger);
            background: linear-gradient(90deg, rgba(231,76,60,0.1), var(--accent));
        }

        .birthday-item.soon {
            border-left-color: var(--warning);
        }

        .birthday-avatar {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--secondary);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
        }

        .birthday-info h4 {
            color: var(--primary);
            margin-bottom: 0.25rem;
        }

        .birthday-info p {
            font-size: 0.85rem;
            color: var(--text-light);
        }

        .birthday-date {
            margin-left: auto;
            text-align: right;
        }

        .birthday-date .day {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--secondary);
        }

        .birthday-date .month {
            font-size: 0.75rem;
            color: var(--text-light);
            text-transform: uppercase;
        }

        /* Toast Notification */
        .toast {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            background: var(--primary);
            color: var(--bg-card);
            padding: 1rem 1.5rem;
            border-radius: 12px;
            box-shadow: var(--shadow-hover);
            display: flex;
            align-items: center;
            gap: 0.75rem;
            transform: translateX(400px);
            transition: transform 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            z-index: 1001;
        }

        .toast.show {
            transform: translateX(0);
        }

        .toast i {
            color: var(--secondary);
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            color: var(--text-light);
        }

        .empty-state i {
            font-size: 4rem;
            color: var(--secondary);
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        .empty-state h3 {
            font-family: 'Playfair Display', serif;
            font-size: 1.5rem;
            color: var(--primary);
            margin-bottom: 0.5rem;
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Responsive */
        @media (max-width: 968px) {
            .app-container {
                grid-template-columns: 1fr;
            }
            
            .sidebar {
                position: static;
                max-height: none;
            }
            
            .contacts-grid {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 600px) {
            .header-content {
                flex-direction: column;
                gap: 1rem;
                text-align: center;
            }
            
            .toolbar {
                flex-direction: column;
            }
            
            .search-box {
                width: 100%;
            }

            .templates-grid {
                grid-template-columns: 1fr;
            }

            .import-export-btns {
                grid-template-columns: 1fr;
            }
        }

        /* Hidden file input */
        input[type="file"] {
            display: none;
        }
    </style>
</head>
<body>
    <header>
        <div class="container">
            <div class="header-content">
                <div class="logo-section">
                    <div>
                        <div class="logo">
                            <i class="fas fa-gem"></i>
                            EmpathyCRM
                        </div>
                        <div class="tagline">Digitalización de la Empatía</div>
                    </div>
                </div>
                <div class="header-actions">
                    <button class="icon-btn" onclick="toggleTheme()" title="Cambiar tema">
                        <i class="fas fa-moon" id="themeIcon"></i>
                    </button>
                    <button class="icon-btn" onclick="openBirthdayModal()" title="Cumpleaños">
                        <i class="fas fa-birthday-cake"></i>
                        <span class="badge" id="birthdayBadge" style="display: none;">0</span>
                    </button>
                    <button class="icon-btn" onclick="openSettingsModal()" title="Configuración">
                        <i class="fas fa-cog"></i>
                    </button>
                </div>
            </div>
        </div>
    </header>

    <div class="app-container">
        <aside class="sidebar">
            <div class="form-header">
                <h2>Nuevo Contacto</h2>
                <p>"Los detalles pequeños crean relaciones grandes"</p>
            </div>
            
            <form id="contactForm">
                <div class="form-row">
                    <div class="form-group">
                        <label>Nombre *</label>
                        <input type="text" id="firstName" required placeholder="Ej: María">
                    </div>
                    <div class="form-group">
                        <label>Apellido *</label>
                        <input type="text" id="lastName" required placeholder="Ej: González">
                    </div>
                </div>

                <div class="form-group">
                    <label>Cumpleaños</label>
                    <input type="date" id="birthday">
                </div>

                <div class="form-group">
                    <label>Email</label>
                    <input type="email" id="email" placeholder="maria@ejemplo.com">
                </div>

                <div class="form-group">
                    <label>Teléfono / WhatsApp</label>
                    <input type="tel" id="phone" placeholder="+34 600 000 000">
                </div>

                <div class="form-group">
                    <label>Profesión / Empresa</label>
                    <input type="text" id="profession" placeholder="Ej: Directora de Marketing en...">
                </div>

                <div class="form-group">
                    <label>Categoría</label>
                    <select id="category">
                        <option value="cliente">Cliente</option>
                        <option value="prospecto">Prospecto</option>
                        <option value="partner">Partner</option>
                        <option value="amigo">Amigo</option>
                        <option value="vip">VIP</option>
                        <option value="familia">Familia</option>
                    </select>
                </div>

                <div class="form-group">
                    <label>Detalles Personales Clave</label>
                    <textarea id="personalDetails" placeholder="Hobbies, nombres de hijos, mascotas, retos actuales, preferencias..."></textarea>
                </div>

                <div class="templates-section">
                    <label>Plantillas de mensajes</label>
                    <div class="templates-grid">
                        <button type="button" class="template-btn" onclick="applyTemplate('birthday')">
                            <i class="fas fa-birthday-cake"></i> Cumpleaños
                        </button>
                        <button type="button" class="template-btn" onclick="applyTemplate('followup')">
                            <i class="fas fa-handshake"></i> Seguimiento
                        </button>
                        <button type="button" class="template-btn" onclick="applyTemplate('value')">
                            <i class="fas fa-gift"></i> Aporte valor
                        </button>
                        <button type="button" class="template-btn" onclick="applyTemplate('reconnect')">
                            <i class="fas fa-redo"></i> Reconectar
                        </button>
                    </div>
                </div>

                <div class="form-group" style="margin-top: 1rem;">
                    <label>Último Contacto</label>
                    <input type="date" id="lastContact">
                </div>

                <button type="submit" class="btn" style="margin-top: 1rem;">
                    <i class="fas fa-plus"></i>
                    Guardar Contacto
                </button>
                <button type="button" class="btn btn-secondary" onclick="resetForm()">
                    <i class="fas fa-undo"></i>
                    Limpiar
                </button>
            </form>
        </aside>

        <main class="main-content">
            <div class="toolbar">
                <div class="search-box">
                    <i class="fas fa-search"></i>
                    <input type="text" id="searchInput" placeholder="Buscar contactos...">
                </div>
                <div class="filter-tags">
                    <button class="tag-filter active" data-filter="all">Todos</button>
                    <button class="tag-filter" data-filter="vip">VIP</button>
                    <button class="tag-filter" data-filter="cliente">Clientes</button>
                    <button class="tag-filter" data-filter="prospecto">Prospectos</button>
                    <button class="tag-filter" data-filter="familia">Familia</button>
                </div>
            </div>

            <div id="contactsList" class="contacts-grid"></div>

            <div id="emptyState" class="empty-state" style="display: none;">
                <i class="fas fa-address-book"></i>
                <h3>No hay contactos aún</h3>
                <p>Comienza a construir relaciones significativas añadiendo tu primer contacto</p>
            </div>
        </main>
    </div>

    <!-- Modal Grabación -->
    <div id="recordModal" class="modal-overlay">
        <div class="modal">
            <button class="close-modal" onclick="closeModal('recordModal')">
                <i class="fas fa-times"></i>
            </button>
            
            <div class="modal-header">
                <h2>Mensaje Personal</h2>
                <p>Graba un mensaje de voz de 20 segundos</p>
            </div>

            <div class="recording-interface">
                <button class="record-button" id="recordBtn" onclick="toggleRecording()">
                    <i class="fas fa-microphone"></i>
                </button>
                
                <div class="timer" id="timer">00:00</div>
                
                <div class="modal-actions">
                    <button class="btn-cancel" onclick="closeModal('recordModal')">Cancelar</button>
                    <button class="btn-send" onclick="sendMessage()">
                        <i class="fas fa-paper-plane"></i>
                        Enviar
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal Configuración -->
    <div id="settingsModal" class="modal-overlay">
        <div class="modal">
            <button class="close-modal" onclick="closeModal('settingsModal')">
                <i class="fas fa-times"></i>
            </button>
            
            <div class="modal-header">
                <h2>Configuración</h2>
                <p>Personaliza tu experiencia</p>
            </div>

            <div class="settings-section">
                <h3><i class="fas fa-palette"></i> Apariencia</h3>
                <div class="setting-item">
                    <span>Modo oscuro</span>
                    <div class="toggle-switch" id="darkModeToggle" onclick="toggleTheme()"></div>
                </div>
            </div>

            <div class="settings-section">
                <h3><i class="fas fa-bell"></i> Notificaciones</h3>
                <div class="setting-item">
                    <span>Recordatorios de cumpleaños</span>
                    <div class="toggle-switch active" id="notifToggle" onclick="toggleNotifications()"></div>
                </div>
                <p style="font-size: 0.8rem; color: var(--text-light); margin-top: 0.5rem;">
                    Recibe notificaciones del navegador cuando alguien cumpla años
                </p>
            </div>

            <div class="settings-section">
                <h3><i class="fas fa-database"></i> Backup de datos</h3>
                <div class="import-export-btns">
                    <button onclick="exportData()">
                        <i class="fas fa-download"></i>
                        <span>Exportar JSON</span>
                    </button>
                    <button onclick="document.getElementById('importFile').click()">
                        <i class="fas fa-upload"></i>
                        <span>Importar JSON</span>
                    </button>
                </div>
                <input type="file" id="importFile" accept=".json" onchange="importData(this)">
                <p style="font-size: 0.8rem; color: var(--text-light); margin-top: 0.5rem; text-align: center;">
                    Exporta tus datos regularmente para no perderlos al limpiar el navegador
                </p>
            </div>
        </div>
    </div>

    <!-- Modal Cumpleaños -->
    <div id="birthdayModal" class="modal-overlay">
        <div class="modal">
            <button class="close-modal" onclick="closeModal('birthdayModal')">
                <i class="fas fa-times"></i>
            </button>
            
            <div class="modal-header">
                <h2><i class="fas fa-birthday-cake" style="color: var(--secondary);"></i> Próximos Cumpleaños</h2>
                <p>No olvides felicitar a tus contactos importantes</p>
            </div>

            <div class="birthday-list" id="birthdayList">
                <!-- Se renderiza dinámicamente -->
            </div>
        </div>
    </div>

    <!-- Toast -->
    <div id="toast" class="toast">
        <i class="fas fa-check-circle"></i>
        <span id="toastMessage">Operación exitosa</span>
    </div>

    <script>
        // Estado global
        let contacts = JSON.parse(localStorage.getItem('empathyCRM_contacts')) || [];
        let settings = JSON.parse(localStorage.getItem('empathyCRM_settings')) || {
            darkMode: false,
            notifications: true
        };
        let currentFilter = 'all';
        let mediaRecorder = null;
        let audioChunks = [];
        let recordingStartTime = null;
        let timerInterval = null;
        let currentContactId = null;

        // Plantillas de mensajes
        const templates = {
            birthday: `¡Feliz cumpleaños {nombre}! 🎉\n\nEspero que tengas un día maravilloso rodeado de tus seres queridos. Que este nuevo año venga cargado de éxitos y momentos inolvidables.\n\nUn abrazo,`,
            
            followup: `Hola {nombre},\n\nQuería contactarte para ver cómo va todo con [tema específico]. Me acordé de ti porque [detalle personal].\n\n¿Tienes un momento para hablar esta semana?\n\nSaludos,`,
            
            value: `Hola {nombre},\n\nVi [artículo/recurso] y pensé inmediatamente en ti porque sé que te interesa [tema relacionado con detalle personal].\n\nEspero te sea útil. ¡Cuéntame qué te parece!\n\nUn saludo,`,
            
            reconnect: `Hola {nombre},\n\nHa pasado tiempo desde la última vez que hablamos. Me preguntaba cómo te va con [proyecto/reto mencionado anteriormente].\n\nMe encantaría ponernos al día. ¿Qué tal tu disponibilidad esta semana?\n\nAbrazos,`
        };

        // Inicialización
        document.addEventListener('DOMContentLoaded', () => {
            loadSettings();
            renderContacts();
            updateStats();
            checkBirthdays();
            
            // Verificar cumpleaños cada hora
            setInterval(checkBirthdays, 3600000);
            
            // Configurar fecha de hoy
            document.getElementById('lastContact').valueAsDate = new Date();
            
            // Solicitar permiso de notificaciones
            if (settings.notifications && 'Notification' in window) {
                Notification.requestPermission();
            }
        });

        // Configuración inicial
        function loadSettings() {
            if (settings.darkMode) {
                document.documentElement.setAttribute('data-theme', 'dark');
                document.getElementById('themeIcon').classList.replace('fa-moon', 'fa-sun');
                document.getElementById('darkModeToggle').classList.add('active');
            }
        }

        function toggleTheme() {
            settings.darkMode = !settings.darkMode;
            localStorage.setItem('empathyCRM_settings', JSON.stringify(settings));
            
            if (settings.darkMode) {
                document.documentElement.setAttribute('data-theme', 'dark');
                document.getElementById('themeIcon').classList.replace('fa-moon', 'fa-sun');
                document.getElementById('darkModeToggle').classList.add('active');
            } else {
                document.documentElement.setAttribute('data-theme', 'light');
                document.getElementById('themeIcon').classList.replace('fa-sun', 'fa-moon');
                document.getElementById('darkModeToggle').classList.remove('active');
            }
        }

        function toggleNotifications() {
            settings.notifications = !settings.notifications;
            localStorage.setItem('empathyCRM_settings', JSON.stringify(settings));
            
            const toggle = document.getElementById('notifToggle');
            toggle.classList.toggle('active');
            
            if (settings.notifications && 'Notification' in window) {
                Notification.requestPermission();
            }
            
            showToast(settings.notifications ? 'Notificaciones activadas' : 'Notificaciones desactivadas');
        }

        // Gestión de contactos
        document.getElementById('contactForm').addEventListener('submit', (e) => {
            e.preventDefault();
            addContact();
        });

        document.getElementById('searchInput').addEventListener('input', (e) => {
            renderContacts(e.target.value);
        });

        document.querySelectorAll('.tag-filter').forEach(btn => {
            btn.addEventListener('click', () => {
                document.querySelectorAll('.tag-filter').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                currentFilter = btn.dataset.filter;
                renderContacts();
            });
        });

        function addContact() {
            const contact = {
                id: Date.now(),
                firstName: document.getElementById('firstName').value,
                lastName: document.getElementById('lastName').value,
                birthday: document.getElementById('birthday').value,
                email: document.getElementById('email').value,
                phone: document.getElementById('phone').value,
                profession: document.getElementById('profession').value,
                category: document.getElementById('category').value,
                personalDetails: document.getElementById('personalDetails').value,
                lastContact: document.getElementById('lastContact').value,
                createdAt: new Date().toISOString()
            };

            contacts.push(contact);
            saveContacts();
            renderContacts();
            updateStats();
            checkBirthdays();
            resetForm();
            showToast('Contacto guardado con éxito');
        }

        function deleteContact(id) {
            if (confirm('¿Estás seguro de que deseas eliminar este contacto?')) {
                contacts = contacts.filter(c => c.id !== id);
                saveContacts();
                renderContacts();
                updateStats();
                checkBirthdays();
                showToast('Contacto eliminado');
            }
        }

        function saveContacts() {
            localStorage.setItem('empathyCRM_contacts', JSON.stringify(contacts));
        }

        function resetForm() {
            document.getElementById('contactForm').reset();
            document.getElementById('lastContact').valueAsDate = new Date();
        }

        // Plantillas
        function applyTemplate(type) {
            const firstName = document.getElementById('firstName').value || '{nombre}';
            const personalDetails = document.getElementById('personalDetails').value || '[detalles personales]';
            
            let template = templates[type]
                .replace(/{nombre}/g, firstName)
                .replace(/\[detalle personal\]/g, personalDetails);
            
            // Copiar al portapapeles
            navigator.clipboard.writeText(template).then(() => {
                showToast('Plantilla copiada al portapapeles');
            });
            
            // También mostrar en un alert para que el usuario la vea
            setTimeout(() => {
                if (confirm('Plantilla copiada. ¿Deseas ver el mensaje completo?')) {
                    alert(template);
                }
            }, 100);
        }

        // Renderizado
        function renderContacts(searchTerm = '') {
            const container = document.getElementById('contactsList');
            const emptyState = document.getElementById('emptyState');
            
            let filtered = contacts;

            if (currentFilter !== 'all') {
                filtered = filtered.filter(c => c.category === currentFilter);
            }

            if (searchTerm) {
                const term = searchTerm.toLowerCase();
                filtered = filtered.filter(c => 
                    c.firstName.toLowerCase().includes(term) ||
                    c.lastName.toLowerCase().includes(term) ||
                    c.profession.toLowerCase().includes(term) ||
                    c.personalDetails.toLowerCase().includes(term)
                );
            }

            filtered.sort((a, b) => new Date(b.lastContact) - new Date(a.lastContact));

            if (filtered.length === 0) {
                container.innerHTML = '';
                emptyState.style.display = 'block';
                return;
            }

            emptyState.style.display = 'none';
            container.innerHTML = filtered.map((contact, index) => {
                const initials = `${contact.firstName[0]}${contact.lastName[0]}`.toUpperCase();
                const lastContactDate = contact.lastContact ? 
                    new Date(contact.lastContact).toLocaleDateString('es-ES', { 
                        day: 'numeric', 
                        month: 'short', 
                        year: 'numeric' 
                    }) : 'Sin contacto';
                
                const daysSinceContact = contact.lastContact ? 
                    Math.floor((new Date() - new Date(contact.lastContact)) / (1000 * 60 * 60 * 24)) : 
                    null;

                let urgencyBadge = '';
                if (daysSinceContact > 30) {
                    urgencyBadge = '<span style="color: var(--danger); font-size: 0.75rem; font-weight: 600;">¡Contactar!</span>';
                }

                // Verificar cumpleaños
                let birthdayBadge = '';
                if (contact.birthday) {
                    const today = new Date();
                    const birthDate = new Date(contact.birthday);
                    const isToday = today.getDate() === birthDate.getDate() && 
                                   today.getMonth() === birthDate.getMonth();
                    
                    if (isToday) {
                        birthdayBadge = '<div class="birthday-badge">¡Hoy!</div>';
                    }
                }

                return `
                    <div class="contact-card" style="animation-delay: ${index * 0.1}s">
                        ${birthdayBadge}
                        <div class="card-header">
                            <div class="contact-info">
                                <h3>${contact.firstName} ${contact.lastName}</h3>
                                <div class="contact-role">${contact.profession || 'Sin profesión'}</div>
                                <span style="display: inline-block; margin-top: 0.5rem; padding: 0.25rem 0.75rem; background: var(--accent); border-radius: 12px; font-size: 0.75rem; text-transform: uppercase; font-weight: 600; color: var(--text-light);">
                                    ${contact.category}
                                </span>
                            </div>
                            <div class="contact-avatar">${initials}</div>
                        </div>
                        
                        <div class="card-body">
                            ${contact.phone ? `
                                <div class="detail-item">
                                    <i class="fas fa-phone"></i>
                                    <span>${contact.phone}</span>
                                </div>
                            ` : ''}
                            
                            ${contact.email ? `
                                <div class="detail-item">
                                    <i class="fas fa-envelope"></i>
                                    <span>${contact.email}</span>
                                </div>
                            ` : ''}

                            ${contact.birthday ? `
                                <div class="detail-item">
                                    <i class="fas fa-birthday-cake"></i>
                                    <span>${new Date(contact.birthday).toLocaleDateString('es-ES', {day: 'numeric', month: 'long'})}</span>
                                </div>
                            ` : ''}

                            ${contact.personalDetails ? `
                                <div class="personal-details">
                                    <h4><i class="fas fa-heart"></i> Detalles Importantes</h4>
                                    <p>${contact.personalDetails}</p>
                                </div>
                            ` : ''}
                        </div>

                        <div class="card-footer">
                            <div class="last-contact">
                                <i class="far fa-clock"></i> Último contacto: ${lastContactDate}
                                ${urgencyBadge}
                            </div>
                            <div class="card-actions">
                                <button class="action-btn" onclick="openRecordModal(${contact.id})" title="Grabar mensaje">
                                    <i class="fas fa-microphone"></i>
                                </button>
                                <button class="action-btn" onclick="copyTemplateForContact(${contact.id})" title="Copiar plantilla">
                                    <i class="fas fa-copy"></i>
                                </button>
                                <button class="action-btn" onclick="window.open('https://wa.me/${contact.phone?.replace(/\D/g, '')}', '_blank')" title="WhatsApp">
                                    <i class="fab fa-whatsapp"></i>
                                </button>
                                <button class="action-btn delete" onclick="deleteContact(${contact.id})" title="Eliminar">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                `;
            }).join('');
        }

        function copyTemplateForContact(id) {
            const contact = contacts.find(c => c.id === id);
            if (!contact) return;
            
            const template = `Hola ${contact.firstName},\n\nEspero que estés teniendo un gran día. Me acordé de ti porque ${contact.personalDetails || 'quiero mantenernos en contacto'}.\n\n¿Cómo va todo por tu lado?\n\nUn abrazo,`;
            
            navigator.clipboard.writeText(template).then(() => {
                showToast(`Mensaje personalizado para ${contact.firstName} copiado`);
            });
        }

        function updateStats() {
            // Actualizar contadores en el header si los hubiera
        }

        // Cumpleaños
        function checkBirthdays() {
            const today = new Date();
            const upcoming = [];
            
            contacts.forEach(contact => {
                if (!contact.birthday) return;
                
                const birthDate = new Date(contact.birthday);
                const isToday = today.getDate() === birthDate.getDate() && 
                               today.getMonth() === birthDate.getMonth();
                
                // Calcular próximo cumpleaños
                const nextBirthday = new Date(today.getFullYear(), birthDate.getMonth(), birthDate.getDate());
                if (nextBirthday < today) {
                    nextBirthday.setFullYear(today.getFullYear() + 1);
                }
                
                const daysUntil = Math.floor((nextBirthday - today) / (1000 * 60 * 60 * 24));
                
                if (isToday || daysUntil <= 7) {
                    upcoming.push({
                        ...contact,
                        isToday,
                        daysUntil
                    });
                    
                    // Enviar notificación del navegador
                    if (isToday && settings.notifications && 'Notification' in window && Notification.permission === 'granted') {
                        new Notification('¡Cumpleaños hoy! 🎂', {
                            body: `${contact.firstName} ${contact.lastName} cumple años hoy. ¡No olvides felicitarle!`,
                            icon: 'https://cdn-icons-png.flaticon.com/512/2926/2926319.png'
                        });
                    }
                }
            });
            
            // Actualizar badge
            const badge = document.getElementById('birthdayBadge');
            const todayBirthdays = upcoming.filter(u => u.isToday).length;
            if (todayBirthdays > 0) {
                badge.textContent = todayBirthdays;
                badge.style.display = 'flex';
            } else {
                badge.style.display = 'none';
            }
            
            return upcoming;
        }

        function openBirthdayModal() {
            const upcoming = checkBirthdays();
            const list = document.getElementById('birthdayList');
            
            if (upcoming.length === 0) {
                list.innerHTML = `
                    <div style="text-align: center; padding: 2rem; color: var(--text-light);">
                        <i class="fas fa-calendar-check" style="font-size: 3rem; margin-bottom: 1rem; opacity: 0.5;"></i>
                        <p>No hay cumpleaños próximos</p>
                    </div>
                `;
            } else {
                list.innerHTML = upcoming.sort((a, b) => a.daysUntil - b.daysUntil).map(contact => {
                    const initials = `${contact.firstName[0]}${contact.lastName[0]}`.toUpperCase();
                    const date = new Date(contact.birthday);
                    
                    return `
                        <div class="birthday-item ${contact.isToday ? 'today' : contact.daysUntil <= 3 ? 'soon' : ''}">
                            <div class="birthday-avatar">${initials}</div>
                            <div class="birthday-info">
                                <h4>${contact.firstName} ${contact.lastName}</h4>
                                <p>${contact.profession || 'Sin profesión'}</p>
                                ${contact.isToday ? '<span style="color: var(--danger); font-size: 0.8rem; font-weight: 600;">¡Hoy!</span>' : 
                                  `<span style="color: var(--text-light); font-size: 0.8rem;">En ${contact.daysUntil} días</span>`}
                            </div>
                            <div class="birthday-date">
                                <div class="day">${date.getDate()}</div>
                                <div class="month">${date.toLocaleDateString('es-ES', {month: 'short'})}</div>
                            </div>
                        </div>
                    `;
                }).join('');
            }
            
            document.getElementById('birthdayModal').classList.add('active');
        }

        // Grabación de voz
        function openRecordModal(contactId) {
            currentContactId = contactId;
            document.getElementById('recordModal').classList.add('active');
        }

        function closeModal(modalId) {
            document.getElementById(modalId).classList.remove('active');
            if (modalId === 'recordModal' && mediaRecorder && mediaRecorder.state === 'recording') {
                mediaRecorder.stop();
                resetRecording();
            }
        }

        function resetRecording() {
            document.getElementById('recordBtn').classList.remove('recording');
            document.getElementById('timer').textContent = '00:00';
            clearInterval(timerInterval);
            audioChunks = [];
        }

        async function toggleRecording() {
            const btn = document.getElementById('recordBtn');
            
            if (!mediaRecorder || mediaRecorder.state === 'inactive') {
                try {
                    const stream = await navigator.mediaDevices.getUserMedia({ audio: true });
                    mediaRecorder = new MediaRecorder(stream);
                    
                    mediaRecorder.ondataavailable = (e) => {
                        audioChunks.push(e.data);
                    };

                    mediaRecorder.onstop = () => {
                        const audioBlob = new Blob(audioChunks, { type: 'audio/wav' });
                        const audioUrl = URL.createObjectURL(audioBlob);
                        console.log('Grabación completada:', audioUrl);
                    };

                    mediaRecorder.start();
                    btn.classList.add('recording');
                    
                    recordingStartTime = Date.now();
                    timerInterval = setInterval(updateTimer, 1000);
                    
                } catch (err) {
                    showToast('Error al acceder al micrófono');
                    console.error(err);
                }
            } else {
                mediaRecorder.stop();
                btn.classList.remove('recording');
                clearInterval(timerInterval);
                mediaRecorder.stream.getTracks().forEach(track => track.stop());
            }
        }

        function updateTimer() {
            const elapsed = Math.floor((Date.now() - recordingStartTime) / 1000);
            const minutes = Math.floor(elapsed / 60).toString().padStart(2, '0');
            const seconds = (elapsed % 60).toString().padStart(2, '0');
            document.getElementById('timer').textContent = `${minutes}:${seconds}`;
            
            if (elapsed >= 20) {
                toggleRecording();
                showToast('Límite de 20 segundos alcanzado');
            }
        }

        function sendMessage() {
            if (audioChunks.length === 0) {
                showToast('Primero graba un mensaje');
                return;
            }
            
            const contact = contacts.find(c => c.id === currentContactId);
            if (contact) {
                contact.lastContact = new Date().toISOString().split('T')[0];
                saveContacts();
                renderContacts();
            }
            
            showToast('¡Mensaje enviado con éxito!');
            closeModal('recordModal');
        }

        // Exportar/Importar
        function exportData() {
            const dataStr = JSON.stringify(contacts, null, 2);
            const dataBlob = new Blob([dataStr], {type: 'application/json'});
            const url = URL.createObjectURL(dataBlob);
            const link = document.createElement('a');
            link.href = url;
            link.download = `empathyCRM_backup_${new Date().toISOString().split('T')[0]}.json`;
            link.click();
            URL.revokeObjectURL(url);
            showToast('Datos exportados correctamente');
        }

        function importData(input) {
            const file = input.files[0];
            if (!file) return;
            
            const reader = new FileReader();
            reader.onload = (e) => {
                try {
                    const imported = JSON.parse(e.target.result);
                    if (Array.isArray(imported)) {
                        if (confirm(`¿Importar ${imported.length} contactos? Esto reemplazará tus contactos actuales (${contacts.length}).`)) {
                            contacts = imported;
                            saveContacts();
                            renderContacts();
                            updateStats();
                            checkBirthdays();
                            showToast('Datos importados correctamente');
                        }
                    } else {
                        throw new Error('Formato inválido');
                    }
                } catch (err) {
                    showToast('Error al importar: archivo inválido');
                    console.error(err);
                }
            };
            reader.readAsText(file);
            input.value = ''; // Reset input
        }

        function openSettingsModal() {
            document.getElementById('settingsModal').classList.add('active');
        }

        // Toast
        function showToast(message) {
            const toast = document.getElementById('toast');
            document.getElementById('toastMessage').textContent = message;
            toast.classList.add('show');
            
            setTimeout(() => {
                toast.classList.remove('show');
            }, 3000);
        }

        // Cerrar modales al hacer click fuera
        document.querySelectorAll('.modal-overlay').forEach(modal => {
            modal.addEventListener('click', (e) => {
                if (e.target === e.currentTarget) {
                    modal.classList.remove('active');
                    if (modal.id === 'recordModal') resetRecording();
                }
            });
        });
    </script>
</body>
</html>
